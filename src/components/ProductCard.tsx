import { Link } from 'react-router-dom'
import type { Product } from '../domain/product'
export const money = (value: number) => new Intl.NumberFormat('pt-BR', { style: 'currency', currency: 'BRL' }).format(value)
export function ProductCard({ product }: { product: Product }) { return <article className="product-card"><Link to={`/produto/${product.slug}`} aria-label={`Ver detalhes de ${product.title}`}><div className="product-image"><img src={product.images[0]} alt={product.title} loading="lazy" decoding="async" width="560" height="480"/><span>Ver produto</span></div><div className="product-copy"><small>{product.attributes.Marketplace}</small><h3>{product.title}</h3><strong>{money(product.price)}</strong>{product.stock > 0 && <span className="stock">Disponível</span>}</div></Link></article> }
